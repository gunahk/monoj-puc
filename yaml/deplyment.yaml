---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tomcat-deployment
  labels:
    app: tomcat
  annotations:
    flux.weave.works/automated_user: Guna HK <guna.hk444@gmail.com>
    flux.weave.works/automated_msg: test
    flux.weave.works/automated: 'true'
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tomcat
  template:
    metadata:
      labels:
        app: tomcat
    spec:
      containers:
      - name: tomcat
        image: gunahk/gitops:sha-48ad84d
        ports:
        - containerPort: 8080
